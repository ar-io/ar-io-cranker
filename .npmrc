# Previously pinned the deprecated @ar-io (hyphen) scope to GitHub Packages
# with private auth. We now consume @ar.io (period) from the public npmjs.org
# registry, so no per-scope override is needed.
